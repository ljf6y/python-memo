確認観点の表に塗りつぶしがなく白の箇所がいくつかあったため、すべて水色で塗りつぶしてください。

No4～No6がMS PJロジックになっているため、BIZ UDPロジックで統一してください。
内部設計書でも同じ指摘が複数回あったため、注意するようお願いします。

No.0として、環境のバージョンの証跡（ハードコピー）を取得してください。
取得内容と理由はNo3と同じですので、そちらを確認してください。

確認観点の表に塗りつぶしがなく白の箇所がいくつかあったため、すべて水色で塗りつぶしてください。

No1、3、4のようにデータを取得する場合は、DBダンプを証跡に残す必要がないでしょうか。

テスト用の正常ファイルや不正ファイルを使用する場合、それも証跡として残す必要がないでしょうか。

No5はエラーメッセージがダウンロードされたEXCELファイルも証跡として必要ではないでしょうか。

「Browse files」の確認の空欄と上記以外の文の最後が「なると」となっているので、「なること」に修正お願いします。

別紙_想定外エラー確認観点の項目にケースと結果がないですが、必要ないでしょうか。

■初期画面表示確認
テーブルを使う観点が抜けている。

■初期画面表示確認
証跡としてDBダンプが必要。

■ファイル選択後の確認
テーブルを使う観点が抜けている。

■ファイル選択後の確認
証跡としてDBダンプが必要。

■「実行」ボタン押下時確認
実行ボタン押下時の処理として正しいことを確認する観点がありません。

■「実行」ボタン押下時確認
テーブルを使う観点が抜けている。

■「実行」ボタン押下時確認
証跡としてDBダンプが必要。

テーブル表示の確認は行わないのか。

フォーマットリストではなくフォーマットリストにしてください。

「5.フォーマットリスト」は数字始まりだと章立てと区別がつかないので、
他のシートに合わせて「■5.表フォーマットリスト」としてください。
表中のフォーマット名もフォーマット名としてください。

「フォーマットドロップダウンリストの選択値更新時は各ボタンの活性／非活性の切り替え確認になるのでは？」

No.1～No.41について「メッセージを表示していること」ではなく
正常時に確認すべきことはないのか。

No.8だけUIの背景色が白いのは意味があるのか。

■ファイル名チェック（正常系）
ファイル名に「異常」が含まれていると誤解してしまう。

■ファイル名チェック（正常系）
No.11はエラーメッセージを指定しているが、他のエラーメッセージがでるということなのか。

No.12とNo.21はエラーが何個の場合でもExcel出力できる事になってますが正しいケースでしょうか？

また10の場合方ということ → 10≦と10＞となっている。

どこに記載されているかが不明だったため、全般として指摘を挙げています。

■ファイル名のチェックについて
内部コードが4桁の場合だった時の確認や基準年月のフォーマットの確認とはどこで確認しているのか。
3桁でも数値以外が入っていた場合はエラーとなるのか。

また、ファイル名は完全一致している必要があるのか。
正しいファイル名＋ユーザの任意の名称でも取り込み可能ではなかったか。
これらのチェックパターンが漏れているのか。

内部コードが1000のものしかケースには内容だが、
100のデータが既にあった場合や、
100でデータ投入 → 120でデータ投入 → 100でデータ投入した場合など、
どのようなときにエラーとなるのか。

正常に行くならどのようなデータ状態になるのか、などのテストはどこで検討しているのか。

No.0 テスト環境の確認
データベース名がない

No.1 ファイル選択後の確認
No.2 「実行」ボタン押下後の確認
No.2 対象テーブルと関連するテーブルが異なりますが、インプットと証跡の違いでしょうか？

No.1アップロード情報の項目数が20項目ですが、
項目数が20であることの確認をするのであれば（No.19、20、21）のケースを行うべきです。

最初のチェックでは同様のエラーとなるが、
次のチェックでは異なるエラーとなるという想定ですが、アップロード例でテスト実施予定ならこちらには不要。

それぞれのチェックで指摘や個別のエラーメッセージを出力することになってますが、
実施内容としてはどの値とどの値で実施する予定でしょうか？

確認値は1つではなく共通値で実施するのでしょうか？

「SC_5_04_02_UT指示書兼結果記録票アップロード（自事業者利用）」xlsx の
全て同様の指摘です。
